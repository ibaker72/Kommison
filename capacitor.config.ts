type CapacitorConfig = {
  appId: string;
  appName: string;
  webDir: string;
};

const config: CapacitorConfig = {
  appId: 'com.voltgrid.app',
  appName: 'VoltGrid',
  webDir: 'out',
};

export default config;
