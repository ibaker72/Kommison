{
  "name": "kommison-root",
  "private": true,
  "scripts": {
    "dev": "cd kommison && npm run dev",
    "build": "cd kommison && npm run build",
    "vercel-build": "npm --prefix kommison install --include=dev && npm --prefix kommison run build && rm -rf .next && cp -R kommison/.next .next"
  },
  "dependencies": {
    "next": "16.1.6"
  }
}
